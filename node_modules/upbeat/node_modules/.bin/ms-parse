#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mochiscript/bin/ms-parse" "$@"
  ret=$?
else 
  node  "$basedir/../mochiscript/bin/ms-parse" "$@"
  ret=$?
fi
exit $ret
