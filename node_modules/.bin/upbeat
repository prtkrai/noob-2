#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../upbeat/bin/upbeat" "$@"
  ret=$?
else 
  node  "$basedir/../upbeat/bin/upbeat" "$@"
  ret=$?
fi
exit $ret
